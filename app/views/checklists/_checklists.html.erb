<ul class="checklists ui-sortable" data-issue-id="<%= issue.id %>">
  <% checklists.each do |checklist| %>
    <%= render :partial => "checklists/checklist", :locals => { :checklist => checklist } %>
  <% end %>
</ul>
