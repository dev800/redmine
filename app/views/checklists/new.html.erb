<%= render :partial => "checklists/new", :locals => {
  :issue => @issue,
  :project => @project,
  :checklist => @checklist,
  :allowed_statuses => @allowed_statuses,
  :priorities => @priorities
} %>
